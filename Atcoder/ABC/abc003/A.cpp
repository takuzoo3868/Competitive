#include <bits/stdc++.h>

#define OUT(d) std::cout<<d;
#define TAB std::cout<<"\t";
#define BR std::cout<<"\n";
#define REP(i, n) for(int i=0; i<(int)(n); ++i)
#define FOR(i, k, n) for(int i=(k); i<(int)(n); ++i)

using namespace std;

int main() {
    int n;
    cin >> n;

    cout << (n + 1) * 5000 << "\n";
    return 0;
}